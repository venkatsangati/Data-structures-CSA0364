#include<stdio.h>
int main()
{
	int arr[1000],i,n;
	printf("enter the size of array\n");
	scanf("%d",&n);
	printf("enter the elements\n");
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
		if(arr[i]%2==0)
		printf("%d is even \n",arr[i]);
		else
		printf("%d is odd \n",arr[i]);
		}
	return 0;
}
